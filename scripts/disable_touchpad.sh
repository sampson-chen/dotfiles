#!/bin/bash

# set to 0 to re-enable
synclient TouchpadOff=1

# tip: open crontab via $ crontab -e and add the following line for running
# at boot-time
# @reboot ~/dotfiles/scripts/disable_touchpad.sh
